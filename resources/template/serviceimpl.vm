$callback.setFileName($tool.append($tableInfo.name, "ServiceImpl.java"))
$callback.setSavePath($tool.append($callback.savePath, "/service/impl"))

package ${packageName}.service.impl;

import com.yhxd.bgn.common.dao.IMapper;
import com.yhxd.bgn.common.service.AbstractService;
import ${packageName}.bean.${tableInfo.name};
import ${packageName}.dao.${tableInfo.name}Mapper;
import ${packageName}.query.${tableInfo.name}Query;
import ${packageName}.service.${tableInfo.name}Service;
import org.springframework.stereotype.Service;

import javax.annotation.Resource;

/**
* $!{tableInfo.comment}(${tableInfo.name})表服务实现类
*
* @author $author
* @version 1.0
* @since $tool.datetime("yyyy/M/d H:m")
*/
@Service("$tool.firstLowerCase($tableInfo.name)Service")
public class ${tableInfo.name}ServiceImpl extends AbstractService<${tableInfo.name},${tableInfo.name}Query> implements ${tableInfo.name}Service {
    /**
     * $!{tableInfo.comment}Mapper
     */
    @Resource
    private ${tableInfo.name}Mapper $tool.firstLowerCase($tableInfo.name)Mapper;

    /**
     * 获取mapper
     *
     * @return {@link IMapper}
     */
    @Override
    protected IMapper<${tableInfo.name}, ${tableInfo.name}Query> getMapper() {
        return this.$tool.firstLowerCase($tableInfo.name)Mapper;
    }

}
